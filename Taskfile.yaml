version: 3

tasks:

  test: 
    desc: Run unit tests
    cmds:
      - hatch run dev:test --verbose

  check:
    desc: Run code quality checks
    cmds:
      - hatch run dev:check

  lint:
    desc: Run linters
    cmds:
      - hatch run dev:lint
